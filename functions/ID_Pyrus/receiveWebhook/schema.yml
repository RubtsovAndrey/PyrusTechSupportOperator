---
id: receiveWebhook
name: receiveWebhook
description: "Validates the Pyrus webhook payload (task_id, allowlisted api_url,\
  \ token), takes an owned idempotency lock, stores the request-scoped Pyrus data in\
  \ the task document, rebuilds the dialog history and resolves the stage to enter."
response-description: "{ taskId, stage, skip, lockToken, reason }"
parameters: []
language: JAVASCRIPT
