---
id: routeStage
name: routeStage
description: Reads dialog state from DB by taskId and returns the current stage name.
  Used for graph routing.
response-description: "{ stage }"
parameters:
- name: taskId
  type: STRING
  required: false
  description: ID of the Pyrus task (defaults to AgentContext taskId)
  properties: null
  items: null
language: JAVASCRIPT
