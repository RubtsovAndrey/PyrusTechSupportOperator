---
id: cond_stage_group2
name: confirmation/transferring/escalating/closed?
position:
  x: 2500
  y: 300
next-step: cond_stage_confirmation
next-error-step: null
parameters:
  condition: Context.getLastFunctionResult() && (Context.getLastFunctionResult().stage
    === 'awaiting_confirmation' || Context.getLastFunctionResult().stage === 'transferring')
  false-step: cond_stage_escalating
