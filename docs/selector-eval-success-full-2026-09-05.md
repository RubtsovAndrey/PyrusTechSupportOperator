# Первая живая серия Selector: success-full

Трасса: `/Users/andreyrubtsov/Documents/pyrus_dev_stand/dev_terra_success_full/all-logs-1788633316977.json`.
Начало: 5 сентября 2026, 21:34:44 МСК. Run ID: `eb63fd45-6f01-409e-8e3c-f9cb37cfe1d4`.

## Результат

Исправление маршрута принято живой трассой: вход проверки настроек вызвал
`routeDevTest`, получил `selectorEval: true` и прошёл весь стенд. Диагностические
MCP/LLM-пробы не выполнялись. Все пять вызовов Terra завершились HTTP 200 и `stop`.
Итог: `samples: 5`, `expectedMet: 5`, пять статусов `selected`, `cleanup: ok`.

Во всех пяти повторах выбраны только `c1/p1` из статьи «Сотрудники: личная карточка
сотрудника в Додо ИС». Цитаты одинаковы и содержат инструкцию загрузить квадратное фото
300×300. Просмотр цитат подтверждает связь с фотографией сотрудника; посторонних
статей и подмены сотрудника клиентом нет. Применимость этой инструкции к конкретному
экрану приложения курьера из такого результата не следует; стенд не формирует ответ.

## Контроль входа и состояния

- Все пять параметров запросов `Llm.sendRequest`, включая системный промпт, совпадают.
- Вопрос, шесть кандидатов, порядок и тексты совпадают с исходной успешной трассой
  `050926_astra_avatar_city_terra_fix/all-logs-1788614866974.json` задачи `377354226`.
  Системный промпт также совпадает; идентификаторы задачи/витка заменены синтетическими.
- SHA-256 вопроса и кандидатов пересчитан и совпал с отчётом:
  `f028dd4878ccab9bc83e0656945052b98b6df1b0a551dea52ccab205e8c99f3d`.
- В dev-БД записаны только отчёт этой серии и её синтетическое состояние. В отчёте
  сохранены пять сырых строк ответа и проверенные цитаты; статус `complete`.
- Удалено только `state:eval:eb63fd45-6f01-409e-8e3c-f9cb37cfe1d4`. Действий Pyrus нет.

## Время и токены

Модель: `1000299722-open_ai_gpt56terra-uos`, лимит 8192. По пяти provider spans:

| Повтор | Время запроса, с | Входные токены | Из них cached | Выходные токены |
|---|---:|---:|---:|---:|
| 1 | 2,17 | 10487 | 0 | 158 |
| 2 | 3,63 | 10487 | 10484 | 158 |
| 3 | 2,92 | 10487 | 10484 | 158 |
| 4 | 2,02 | 10487 | 10484 | 158 |
| 5 | 2,80 | 10487 | 10484 | 158 |

Среднее время запроса — 2,71 с. Всего 52435 входных и 790 выходных токенов; из входных
41936 обозначены провайдером как cached. Денежная стоимость здесь не рассчитывается.
Время `elapsedMs` самого стенда немного больше из-за переходов между узлами.

## Вывод и следующий шаг

Стенд и маршрут подтверждены для одной серии. Этот вход дал 5/5 полезных отборов;
результат не объясняет прежний пустой отбор на другом входе и не доказывает общую
устойчивость модели. Следом выполнить `empty-full`, `empty-single`, `noise-only`,
по одной команде с ожиданием `Collect sample 5`. Промпт, модель и набор не менять
до завершения сравнения. Повторять `success-full` сейчас не требуется.
