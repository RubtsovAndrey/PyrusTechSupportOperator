# Первый экспорт dev и проверка настроек

Экспорт `1d682dc` от 5 сентября 2026, 18:33 МСК. Новый проект действительно получил
отдельную идентичность; перемещение блока создало достаточное изменение для экспорта.

| Привязка | Существующий проект main | Новый проект dev |
|---|---|---|
| project-key | `pyrustechsupportoper-1000299722-gyS` | `pyrustechsupportoper-1000299722-YwT` |
| PROJECT DB | `1000299722-pyrus_bot_database-hul` | `1000299722-pyrus_bot_database-lwi` |
| GPT-4.1-mini | `1000299722-open_ai_gpt41mini-tme` | `1000299722-open_ai_gpt41mini-etf` |
| Terra | `1000299722-open_ai_gpt56terra-swl` | `1000299722-open_ai_gpt56terra-uos` |
| CUSTOM credential БЗ | `1000299722-kbmcptoken-vod` | `1000299722-kbmcptoken-sut` |

Новый Pyrus webhook:
`https://agentplatform.just-ai.com/workflow-trigger/e78aebc0-9b6e-42ab-aab1-eaba33ac1a2a`.
К тестовой форме он ещё не подключён по подтверждённым данным сессии.

В функциях и узлах заменены ссылки на БД, модели и credentials. Старых main-ссылок
на БД, CUSTOM credential БЗ, GPT-4.1-mini и Terra в исполняемых файлах dev не обнаружено.
`origin-key` в метаданных интеграций может ссылаться на исходные ресурсы; эти поля
вручную не переписывались. Значения секретов и содержимое БД по экспорту не определяются.

Отдельная `integrations/mcp/...` исчезла при импорте. Текущие `getKnowledgeMcp` и
`findOperatorKnowledge` используют `Http.post` и CUSTOM credential, поэтому удаление
не разрывает их граф. Доступ к серверу требуется проверить отдельно.

Платформа также изменила `settings.default-agent-user-message-source` с null на
`ORIGINAL_USER_MESSAGE`. Это изменение экспорта сохранено и должно быть учтено при
живой проверке разговора; локальные подстановки моделей не доказывают равенства поведения.

Локальные инструменты и тесты теперь определяют ключ credential по зарегистрированному
профилю проекта через `tools/project-bindings.js`, вместо привязки к одному main-ключу.
Исходные исторические трассы и сценарии с main-идентификаторами не переписывались.

## Следующее действие в интерфейсе dev

После отправки подготовленной версии:

1. В `PyrusTechSupportOperator-dev` выполнить **Pull**.
2. Выбрать **Тестировать → Dev - check setup**.
3. В открывшемся тестовом виджете отправить `проверка`, если запуск не произошёл
   автоматически через `/start`.
4. Открыть результат функции **Dev - inspect configuration and access** в панели
   выполнения и передать результат либо выгрузить логи теста.

Для этой проверки публикация и подключение формы Pyrus не нужны. Функция требует
тестовый канал и правильный project-key. При несовпадении остановится до доступа к БД
и интеграциям; если интерфейс запуска передаст другой тип канала, нужно изучить трассу,
а не убирать ограничение вслепую.

## Что проверяет inspectDevSetup

| Поле результата | Что означает успешный результат |
|---|---|
| `config: ok` | Присутствуют только две тестовые формы, нужные роли и handover_only |
| `catalog: present`, `topicCount` | Есть непустой каталог; это ещё не проверка совпадения версии |
| `kbCredential: present` | CUSTOM credential доступен и содержит непустой токен |
| `kbToolDiscovery: ok` | MCP tools/list доступен и объявляет search_content/get_content |
| `defaultModel: ok` | GPT-4.1-mini ответила на минимальный запрос без инструментов |
| `selectorModel: ok` | Terra ответила на такой же запрос |

`missing`, `unavailable`, `review_required` и `failed` показывают недостающую настройку
или отказ. Отчёт не включает значения credentials, тексты статей и сообщения исключений.
Функция не пишет config, state или каталог, не вызывает Pyrus и не меняет чат.
Выполняются два реальных коротких LLM-запроса, каждый с лимитом 512 токенов.

Эта проверка не подтверждает доступ к каждой статье, правильность botAuthorIds,
версию knowledge_catalog, работу полного диалога или готовность к публикации.
Это проверка настройки нового окружения; отдельный стенд повторных запусков Selector
на зафиксированных статьях ещё предстоит добавить.

## Если БД пуста

Подготовленные значения уже находятся в файлах:

- запись `config`: `docs/environments/dev-config.json` как начальная основа;
- запись `knowledge_catalog`: `docs/knowledge_catalog.json` из той же версии ветки.

Сначала получить диагностику. Существующие записи не перезаписывать шаблоном без
сравнения. Идентификаторы бота и настройки полей проверить по текущей тестовой интеграции.
Не переносить state, claims и токены из старых чатов. Значения секретов, если потребуется
их ввести, вводятся непосредственно в платформе.
