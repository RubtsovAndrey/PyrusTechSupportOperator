---
id: agent_routing
name: Routing Agent
position:
  x: 2690
  y: -882
next-step: func_parsejson_routing
next-error-step: func_escalate_m9k4j
parameters:
  llm-model-key: 1000299722-yandex_aliceaillmfla-div
  llm-model-settings:
    max-tokens: 256
    temperature: 0.2
    top-p: 1.0
    frequency-penalty: 0.0
    presence-penalty: 0.0
    verbose-mode: false
    structured-output-enabled: false
    structured-output-schema: null
  system-promts:
    role: Ты — маршрутизатор в поддержке партнёров пиццерий и кофеен.
    goal: "Определи по описанию проблемы, к какой тематике из каталога знаний она\
      \ относится."
    instructions: |
      ОБЩИЕ ПРАВИЛА:
      - Общайся только на русском языке.

      Используй tool searchKnowledge для поиска тематики по описанию проблемы.
      Выбирай только из тематик, которые вернул tool. Поля key, route и componentName копируй из выбранной тематики без изменений.
      Если tool вернул found: false, или ни одна тематика не подходит, или у тебя нет уверенности — верни topicKey: null и route: "escalate". Не выдумывай тематику сам.

      ВАЖНО: верни ТОЛЬКО JSON без markdown-обёртки (без ```json и ```).
      Верни JSON: {"topicKey": "ключ|null", "route": "solver|subtask|escalate|null", "componentName": "строка|null", "reason": "кратко"}
    query-override: Определи тематику проблемы партнёра. Описание проблемы — в блоках
      «Известные данные по обращению» (поле Проблема) и «Текущее сообщение партнёра».
  tools:
  - func_searchknowledge_q3m8x
  handoff-agents:
    allowed-any: false
    allowed-agent-node-ids: []
  stay-in-agent: false
  telephony-config: null
  enable-context: true
  chat-history-limit: 10
  sequential-tool-calls-limit: 3
  unique-tools-count-limit: 1
  total-response-timeout: null
